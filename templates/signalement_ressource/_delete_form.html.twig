<form method="post" action="{{ path('app_signalement_ressource_delete', {'id': signalement_ressource.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement_ressource.id) }}">
    <button class="btn">Delete</button>
</form>
