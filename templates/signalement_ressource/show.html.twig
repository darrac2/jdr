{% extends 'base.html.twig' %}

{% block title %}SignalementRessource{% endblock %}

{% block body %}
    <h1>SignalementRessource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signalement_ressource.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ signalement_ressource.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_signalement_ressource_index') }}">back to list</a>

    <a href="{{ path('app_signalement_ressource_edit', {'id': signalement_ressource.id}) }}">edit</a>

    {{ include('signalement_ressource/_delete_form.html.twig') }}
{% endblock %}
