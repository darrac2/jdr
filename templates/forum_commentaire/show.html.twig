{% extends 'base.html.twig' %}

{% block title %}ForumCommentaire{% endblock %}

{% block body %}
    <h1>ForumCommentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forum_commentaire.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ forum_commentaire.text }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ forum_commentaire.dateCreation ? forum_commentaire.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forum_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_forum_commentaire_edit', {'id': forum_commentaire.id}) }}">edit</a>

    {{ include('forum_commentaire/_delete_form.html.twig') }}
{% endblock %}
