<form method="post" action="{{ path('app_signalementforumcommentaire_delete', {'id': signalementforumcommentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalementforumcommentaire.id) }}">
    <button class="btn">Delete</button>
</form>
