{% extends 'base.html.twig' %}

{% block title %}Signalementforumcommentaire{% endblock %}

{% block body %}
    <h1>Signalementforumcommentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signalementforumcommentaire.id }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ signalementforumcommentaire.sujet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_signalementforumcommentaire_index') }}">back to list</a>

    <a href="{{ path('app_signalementforumcommentaire_edit', {'id': signalementforumcommentaire.id}) }}">edit</a>

    {{ include('signalementforumcommentaire/_delete_form.html.twig') }}
{% endblock %}
