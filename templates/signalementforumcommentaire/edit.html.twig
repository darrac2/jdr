{% extends 'base.html.twig' %}

{% block title %}Edit Signalementforumcommentaire{% endblock %}

{% block body %}
    <h1>Edit Signalementforumcommentaire</h1>

    {{ include('signalementforumcommentaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_signalementforumcommentaire_index') }}">back to list</a>

    {{ include('signalementforumcommentaire/_delete_form.html.twig') }}
{% endblock %}
