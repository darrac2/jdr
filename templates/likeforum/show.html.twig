{% extends 'base.html.twig' %}

{% block title %}Likeforum{% endblock %}

{% block body %}
    <h1>Likeforum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ likeforum.id }}</td>
            </tr>
            <tr>
                <th>Liker</th>
                <td>{{ likeforum.liker ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_likeforum_index') }}">back to list</a>

    <a href="{{ path('app_likeforum_edit', {'id': likeforum.id}) }}">edit</a>

    {{ include('likeforum/_delete_form.html.twig') }}
{% endblock %}
