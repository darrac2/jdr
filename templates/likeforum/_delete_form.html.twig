<form method="post" action="{{ path('app_likeforum_delete', {'id': likeforum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ likeforum.id) }}">
    <button class="btn">Delete</button>
</form>
